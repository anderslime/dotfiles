#!/usr/bin/env bash
# Usage: update_gem_and_pr target_repo gem_to_update

gem_name="$1"
pull_request="$2"

ug "$gem_name"
if [ -n $pull_request ]; then
  echo "Creating Pull Request"
  hub pull-request -b lokalebasen:master -m "Update $gem_name"
fi;
